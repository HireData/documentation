---
title: "Why Didn't My Automation Run?"
sidebarTitle: "Automation Didn't Run"
description: "Diagnose why a HireData automation didn't run: checking event delivery, reading run logs, understanding cancelled runs and safety limits, and replaying events."
---

{/* TODO(docs-reorg): symptom-first diagnosis flow:
   1. Did the event arrive? (Events page) — DONE
   2. Is the automation active? — TODO
   3. Did the event pass the start filters? — TODO
   4. Was a run created and what is its status? (Runs, safety limits, delays)
      — PARTIAL: Send Limits done; Invalid Sender, ghosted/cooling-off and
        Delayed still open, see HD-1687
   5. Replaying the event once fixed — DONE
   Cross-link: /settings/logs/events (replay section), /settings/logs/runs,
   /settings/automation-safety-limits, /automations/how-to-test-an-automation */}

{/* ---------------------------------------------------------------------------
  HD-1687 — partial fill of this placeholder. Author: Callum. Reviewer: Ian.

  Steps 1, 5 and part of 4 are written from verified sources. Steps 2 and 3 need
  product access I don't have yet.

  Step 4 is the HD-1687 work: users paste "Run Cancelled – ..." strings into Ask
  HireData and none of those strings appears anywhere in the KB. Send Limits
  questions resolve anyway because Automation Safety Limits is a good article;
  Invalid Sender Email has no equivalent and doesn't resolve.

  NOT VERIFIED — please confirm before publish:
  1. That "Send Limits Exceeded" maps to the limits in Settings → Safety Limits
    and "Send Limits Per Automation Exceeded" to the per-automation ones.
    Inferred from the error names and from three support threads resolving to
    that article. Not documented; I haven't seen the engine emit either string.
    This is stated flatly in the body prose below — if it's wrong, those two
    subsections are wrong, so it's the first thing to check.
  2. Whether §1.1/§1.2 are genuinely account-scoped and aggregate across
    automations. The docs establish "account-level safety limits" as a category
    without saying which sections are account-scoped.
  3. What "cancelled" means mechanically, and what separates Cancelled from
    Delayed. /settings/logs/events lists both as statuses without defining them.
  4. Where the cancellation reason shows in the UI. /settings/logs/runs documents
    state and sub-state filters, so sub-state is the likely home — not stated.
  5. The separator in the real error string. I've used an en dash; users typed it
    without one. Since this section exists to match what people paste, it matters.
  6. The full list of reasons the engine emits. I have three distinct strings
    across five support conversations (four threads), and seven limit types in
    the docs.
  7. Tab naming: /settings/logs/events lists the event detail tabs as Event Data
    / External Records / Processes, but its replay walkthrough says to open the
    event's "Runs" tab. Step 1 and step 5 below inherit both names. One of them
    is wrong in the source and should be fixed there.

  TRANSLATIONS: /nl/, /de/, /fr/ and /pt/ versions of this page already exist as
  placeholders carrying the same TODO. They'll diverge from this one until they're
  updated — worth deciding whether that happens now or after steps 2 and 3 land.
  --------------------------------------------------------------------------- */}

<Note>
  This article is in progress. Steps 2 and 3 aren't written yet, and the list of run cancellation reasons in step 4 isn't complete. In the meantime, see [Events](/settings/logs/events) for how to inspect and replay events.
</Note>

_When an automation doesn't do what you expected, the cause is usually one of four things: the event never arrived, the automation isn't active, the event didn't pass your filters, or a run started but was cancelled. Work through the checks below in order — each one narrows down where the problem is. Step 5 covers how to replay the event once you've fixed the cause._

## 1. Did the event arrive?

Automations are triggered by events sent from your connected app. If no event arrived, nothing downstream can happen.

Go to **Settings → Events** and filter by the app you expect the event from. If you can find the event, open it and check the **Processes** tab — this lists the automations the event triggered and the runs it created, along with each run's status.

- **No event at all** – the problem is upstream, in the connection rather than the automation. For Carerix, see [Why is my Carerix not syncing with HireData](/apps/carerix/why-is-my-carerix-not-syncing-with-hiredata).
- **Event arrived, no processes listed** – the event likely didn't meet the criteria of any active automation. Continue to steps 2 and 3.
- **Event arrived, a run was created** – skip to step 4.

See [Events](/settings/logs/events) for how to read the event detail view.

{/* ---------------------------------------------------------------------------
  STUB — 2. Is the automation active?

  Needs: where the active/inactive toggle lives, what an inactive automation does
  with incoming events (ignored silently, or queued?), and whether there's any
  visible signal that an automation is off. I couldn't confirm any of this.
  --------------------------------------------------------------------------- */}

## 2. Is the automation active?

_Being written. Until then, see [How to test an automation](/automations/how-to-test-an-automation)._

{/* ---------------------------------------------------------------------------
  STUB — 3. Did the event pass the start filters?

  The Events troubleshooting section says to "Compare the Event Data with the
  conditions in your automation to ensure it matches", which is the shape of this
  step, but I'd want to describe the filter UI accurately rather than paraphrase
  one line. /automations/how-to-test-an-automation's "Preview a Single Item" mode
  looks like the right tool here, but I haven't used it.
  --------------------------------------------------------------------------- */}

## 3. Did the event pass the start filters?

_Being written. Until then, see [How to test an automation](/automations/how-to-test-an-automation)._

## 4. Was a run created, and was it cancelled?

If a run exists but didn't finish, its status tells you why. Go to **Settings → Runs** and filter by state or sub-state, or reach the run from the **Processes** tab of the event. See [Runs](/settings/logs/runs) for how to read a run's detail view.

The cancellation reasons documented so far all relate to safety limits, which exist to prevent an automation from overwhelming the same contact.

### Run Cancelled – Send Limits Exceeded

The recipient has reached one of the limits configured in the **Safety Limits** section:

- **Email send limits** cap how many emails a single email address can receive.
- **Conversation send limits** cap how many conversations are initiated per phone number.

**_What to do:_** _Go to the Safety Limits section in HireData to review the current caps and adjust them to fit your outreach strategy. See [Automation Safety Limits](/settings/automation-safety-limits) for a breakdown of each limit and what it controls._

### Run Cancelled – Send Limits Per Automation Exceeded

The recipient has reached a limit set on that **specific automation**, rather than one of the limits above. Every automation in HireData can carry its own:

- **Email Limit** – how many emails this automation may send to the same email address within the selected time frame.
- **Conversation Limit** – how many new conversation attempts this automation may start for the same recipient within a given period. This applies to automations that initiate conversations via WhatsApp.

**_What to do:_** _Open the automation, click the three vertical dots in the upper right, and choose Settings to review its limits. See [Automation Safety Limits](/settings/automation-safety-limits) for the full breakdown._

<Warning>
  At the bottom of an individual automation's Safety Limits panel you'll find **Ignore Safety Limits**. When enabled, that automation bypasses all safety limits, both its own and the account-level ones. Only use this when you're confident the automation cannot trigger excessively, the recipients expect multiple messages, and your use case genuinely requires unrestricted sending. For most automations we strongly recommend keeping the safety limits enabled.
</Warning>

{/* ---------------------------------------------------------------------------
  STUB — Run Cancelled – Invalid Sender Email

  The highest-value section here and the reason HD-1687 prioritised it. None of
  the three observed cancellation strings appears in the KB, but the other two
  resolve anyway because Automation Safety Limits covers the underlying setting.
  This one has no equivalent, which is why the support conversation asking about
  it ran to 179,619 tokens and came back partial — the analyst's note reads "No
  exact KB article on this error; agent inferred troubleshooting from
  domain/sender docs: check Settings->Domains, verify DNS, sender selection,
  brand settings."

  That inference is a decent hint at the answer, but a hint isn't a source. Not
  writing it until someone confirms what actually triggers the error:

  a) The sending domain isn't verified.
     /settings/domains/how-to-verify-a-domain-in-hiredata-to-send-emails:
     "If your domain is not verified, you will not be able to send emails via
     HireData."
  b) No Default Sender Email is configured. Same article: "this will be the
     default email address used when sending emails if this information is not
     available."
  c) The sender address resolved from the event record is malformed or empty.
  d) Something else.

  @Ian — this is the one I need from you.
  --------------------------------------------------------------------------- */}

{/* ---------------------------------------------------------------------------
  STUB — Ignored, Ghosted, Cooling-off period, and Delayed

  Automation Safety Limits §3.1, §3.2 and §4.1 describe three further behaviours:

  - Ignored (no reply): "Automatically cancels a conversation if there's no
    reply within the set time period"
  - Ghosted (stopped responding): "Cancels a conversation if there's no reply
    after a follow-up within the set time period."
  - Cooling-off period: "Prevents automatic restarts when a contact sends a new
    message too soon after a conversation has closed."

  All three are documented as cancelling CONVERSATIONS. Whether any surfaces as a
  cancelled RUN is what I shouldn't assume, so they're held. If they do, each
  needs its error string first — matching the string is the point.

  Delayed runs are also unwritten. The original TODO mentions delays and
  /settings/logs/events lists Delayed as a run status, but nothing documents what
  causes one or how long it lasts.
  --------------------------------------------------------------------------- */}

## 5. Replaying the event once it's fixed

Once you've corrected the cause, you don't need to wait for the same thing to happen again — you can replay the original event to re-trigger the automation.

<Warning>
  When you replay an event, it can trigger runs across multiple automations at once. Before replaying, always check which automations and runs will be affected, especially if any of them include sending an email or a message. Replaying those without checking first could result in duplicate messages being sent to candidates or contacts.
</Warning>

Open the event, use the three-dot menu and select **Replay**. Before confirming, check which automations were triggered last time and whether any include **Send Email/Message/Survey** tasks.

Replaying behaves differently by automation type: event-based automations re-trigger immediately, while scheduled (one-time) automations keep their planned run date and only re-evaluate whether the event matches their conditions.

See [Events](/settings/logs/events) for the full replay walkthrough, including how to replay several events at once.

## Still need help?

If you've worked through these steps and still aren't sure what happened, contact our team at [support@hiredata.com](mailto:support@hiredata.com). Please include:

- A link or the ID of the event, and of the run if one was created;
- A link or the name of the automation you expected to trigger;
- Any relevant data or context that might help us investigate further.

{/* ---------------------------------------------------------------------------
  BEFORE PUBLISH

  - Steps 2 and 3 need writing, and unknown #1 in the header needs answering.
  - Cross-link from /settings/logs/runs and /settings/automation-safety-limits.
  On the runs page, place the link ahead of the existing support-email line
  rather than replacing it — that line is the page's only support contact.
  - Trim the in-progress <Note> as steps land; remove it entirely when 2 and 3
  are done and the cancellation list is confirmed complete.
  - Update the four existing translations. Keep the error strings in English in
  all of them: the one Dutch Send Limits conversation pasted the English string
  while asking in Dutch.
  --------------------------------------------------------------------------- */}